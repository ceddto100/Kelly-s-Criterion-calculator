# Base URL for the deployed Render backend that the Vite frontend should call.
# Override this value with http://localhost:5000 when developing against a local server.
VITE_API_URL=https://your-render-backend-url.onrender.com
