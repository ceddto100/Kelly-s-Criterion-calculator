# ==================== Server Configuration ====================
PORT=3000
NODE_ENV=development

# ==================== Database ====================
# MongoDB connection string
# Local: mongodb://localhost:27017/betting-calculator
# Production: Use MongoDB Atlas connection string
MONGODB_URI=mongodb://localhost:27017/betting-calculator

# ==================== AI Service Configuration ====================

# Gemini API (for both /api/calculate and /api/analyze endpoints)
# Used for:
#   - Kelly Criterion calculations (/api/calculate)
#   - NBA Sports Matchup Analysis (/api/analyze)
# Get your API key from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# ==================== Sports API Configuration ====================

# API-Sports Basketball API Key
# Used for:
#   - NBA Games schedule and live scores (/api/nba/games)
# Get your API key from: https://www.api-football.com/
APISPORTS_KEY=

# ==================== Security & CORS ====================

# Admin API key for accessing /api/admin/stats endpoint
# Generate a secure random string for production
ADMIN_KEY=your_secure_admin_key_here

# Frontend URL for CORS configuration
# Local development: http://localhost:5173
# Production: Your production deployment URL
FRONTEND_URL=http://localhost:5173

# Production example:
# FRONTEND_URL=https://betgistics.com

# ==================== Google OAuth 2.0 Configuration ====================

# Google OAuth Client ID and Secret
# Get these from: https://console.cloud.google.com/apis/credentials
#
# Setup Steps:
# 1. Go to Google Cloud Console (https://console.cloud.google.com/)
# 2. Create a new project or select an existing one
# 3. Enable the Google+ API
# 4. Go to "Credentials" and create "OAuth 2.0 Client ID"
# 5. Set Application Type: "Web application"
# 6. Add Authorized Redirect URIs:
#    - For local development: http://localhost:3000/auth/google/callback
#    - For production: https://your-domain.com/auth/google/callback
# 7. Copy the Client ID and Client Secret below

GOOGLE_CLIENT_ID=your_google_client_id_here.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# Session secret for encrypting session cookies
# Generate a strong random string (e.g., use: openssl rand -base64 32)
# IMPORTANT: Change this in production!
SESSION_SECRET=your_session_secret_here_change_in_production

# ==================== Rate Limiting ====================

# Time window in milliseconds (default: 15 minutes)
RATE_LIMIT_WINDOW_MS=900000

# Maximum requests per window (default: 100)
RATE_LIMIT_MAX_REQUESTS=100

# ==================== Notes ====================
#
# For Production Deployment (Render):
# 1. Set all API keys as environment variables in Render dashboard
# 2. Update FRONTEND_URL to your Vercel deployment URL
# 3. Update MONGODB_URI to MongoDB Atlas connection string
# 4. Set NODE_ENV=production
# 5. Generate a strong ADMIN_KEY
#
# For Local Development:
# 1. Copy this file to .env
# 2. Replace placeholder values with your actual API keys
# 3. Keep FRONTEND_URL as http://localhost:5173
# 4. Use local MongoDB or MongoDB Atlas for testing
