import React from 'react';
import ReactDOM from 'react-dom/client';
import { NBAGamesPage } from '../components/NBAGamesPage';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <NBAGamesPage />
  </React.StrictMode>
);
